version: '3.8'

services:
  recommendation-db:
    image: postgres:15
    container_name: recommendation-db
    ports:
      - "5432:5432" # Máy tính của bạn : Bên trong Docker
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: recommendation_db
    restart: always